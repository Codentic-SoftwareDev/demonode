<% if (status) {%>
    <% if (message.length> 0) { %>
        <div id="mymessage">
            <ul id="success-msg" style="background-color: green;
        color: white;
        PADDING: 10px;
        list-style-type: none;
        position: absolute;
        text-align: center;
        border-radius: 10px;
        right: 10px;
        z-index: 150;
        top: 20px;">
                <%= message %>
                    <button class="close-message" id="close-alert" style=" border: none;
      background: none;
      color: white;">
                        <span aria-hidden="true">X</span>
                    </button>
            </ul>
        </div>
        <% } %>
            <% } else { %>
                <% if (message.length> 0) { %>
                    <div id="mymessage">
                        <ul id="error-msg" style="background-color: red;
        color: white;
        PADDING: 10px;
        list-style-type: none;
        position: absolute;
        text-align: center;
        border-radius: 10px;
        z-index: 150;
        right: 10px;
        top: 20px;">
                            <%= message %>
                                <button class="close-message" id="close-alert" style=" border: none;
      background: none;
      color: white;">
                                    <span aria-hidden="true">X</span>
                                </button>
                        </ul>
                    </div>
                    <% } %>
                        <% } %>