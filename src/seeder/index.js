const addSuperAdmin = require('./super-admin')
const addLoggerUser = require('./logger-user')
const addSettings = require('./settings')

module.exports = {
  addSuperAdmin,
  addLoggerUser,
  addSettings
}
